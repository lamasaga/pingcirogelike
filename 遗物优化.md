# 遗物与计分系统优化文档

## 1. 核心公式定义

为了统一游戏内的数值逻辑，我们将计分体系拆分为三个独立的计算层级。

### 1.1 字母分数 (Letter Score)
计算单个字母在特定上下文中的有效分值。
$$ \text{LetterFinal} = (\text{Base} + \text{BaseAdd}) \times \text{PercentMult} + \text{TotalAdd} $$

*   **Base (基础分)**: 字母的原始设定分数 (如 A=1, Z=10)。
*   **BaseAdd (基础加值)**: 数值修正 (例如：A01 "元音+2", A04 "常用字母+2")。
*   **PercentMult (倍率修正)**: 乘算修正 (例如：C02 "交叉点字母x3", D06 "首字母x2")。
*   **TotalAdd (最终加值)**: 计算完倍率后的额外加分 (较少使用，预留接口)。

### 1.2 单词分数 (Word Score)
计算一个完整单词的基础总分（在考虑交叉加成之前）。
$$ \text{WordScore} = (\sum \text{LetterFinal} + \text{RootWordBuff}) \times (\text{Length} + \text{LengthBuff}) \times \text{WordMult} $$

*   **Sum LetterFinal**: 单词中所有字母的最终分之和。
*   **RootWordBuff (词根基础加值)**: 直接增加在字母总和上的分数 (例如：C05 "跨中线+10", D01 "元音开头+5")。*注：这部分分数会享受到长度乘区的放大。*
*   **Length (长度)**: 单词的字母数量。
*   **LengthBuff (长度修正)**: 改变参与乘算的长度值 (预留接口，如 "短单词视作+1长度")。
*   **WordMult (单词独立倍率)**: 针对整词的乘法修正 (例如：B01 "长度2-3 x1.5", D05 "回文 x5")。

### 1.3 交叉与最终分数 (Final Score)
处理单词之间的互动加成。
$$ \text{FinalScore} = \text{WordScore} \times (\text{CrossCount} \times \text{CrossCountBuff} + \text{IndependentCrossMult}) $$

*   **CrossCount**: 该单词与其他单词产生的有效交叉点数量。
*   **CrossCountBuff**: 每个交叉点提供的倍率，**基础值为 1** (例如：C01 遗物可使其 +0.5 变为 1.5)。
*   **IndependentCrossMult**: 交叉系统的独立基础倍率，默认为 1。(例如：C04 "与元音交叉时，倍率+0.5")。

**基础交叉倍率示例（无遗物）：**
- 0次交叉：`0 × 1 + 1 = 1倍`
- 1次交叉：`1 × 1 + 1 = 2倍`
- 2次交叉：`2 × 1 + 1 = 3倍`

---

## 2. 遗物重构列表 (Relic List Optimization)

根据上述公式，重新归类和定义现有遗物。

### 第一类：字母强化 (Letter Mastery)
*主要影响 `BaseAdd` 和 `PercentMult`*

| ID | 名称 | 旧效果 | 新定义 (适配公式) | 逻辑层级 |
|:---|:---|:---|:---|:---|
| A01 | 元音护符 | 元音+2 | 元音 **BaseAdd += 2** | Letter |
| A02 | 辅音磨刀石 | 辅音+1 | 辅音 **BaseAdd += 1** | Letter |
| A03 | 重金属 | 稀有+5 | J/K/Q/X/Z **BaseAdd += 5** | Letter |
| A04 | 通用树脂 | 常用+2 | E/S/T/R/N **BaseAdd += 2** | Letter |
| A05 | 黄金 A | A变10分 | A **Base = 10** (覆盖) | Letter |
| A06 | 白银 E | E+5 | E **BaseAdd += 5** | Letter |
| A07 | 双生护符 | 重复字母翻倍 | 若单词含重复字母，该字母 **PercentMult *= 2** | Letter (Context) |

### 第二类：长度策略 (Length Strategy)
*主要影响 `WordMult` 和 `RootWordBuff`*

| ID | 名称 | 旧效果 | 新定义 (适配公式) | 逻辑层级 |
|:---|:---|:---|:---|:---|
| B01 | 短剑 | 长度2-3 x1.5 | 长度2-3 **WordMult *= 1.5** | Word |
| B02 | 长柄斧 | 长度5-6 x1.5 | 长度5-6 **WordMult *= 1.5** | Word |
| B03 | 百科全书 | 长度7+ x2.5 | 长度7+ **WordMult *= 2.5** | Word |
| B04 | 奇数魔杖 | 奇数分+5 | 奇数长度 **RootWordBuff += 5** | Word |
| B05 | 偶数大盾 | 偶数倍率+0.25 | 偶数长度 **WordMult += 0.25** | Word |
| B06 | 斐波那契 | 数列长度 x2 | 符合长度 **WordMult *= 2** | Word |

### 第三类：结构与交叉 (Structure & Cross)
*主要影响 `RootWordBuff`, `WordMult` 及 `Intersection` 公式*

| ID | 名称 | 旧效果 | 新定义 (适配公式) | 逻辑层级 |
|:---|:---|:---|:---|:---|
| C01 | 交通指挥棒 | 交叉+10分 | 每个交叉点 **CrossCountBuff += 0.5** (改为倍率成长) | Intersection |
| C02 | 十字路口 | 交叉字母x3 | 位于交叉点的字母 **PercentMult *= 3** | Letter (Context) |
| C04 | 紧密连接 | 元音交叉倍率+0.5 | 若与元音交叉 **IndependentCrossMult += 0.5** | Intersection |
| C05 | 桥梁工程师 | 跨中线+10 | 跨中线 **RootWordBuff += 10** | Word |
| C06 | 角落蜘蛛 | 触边 x2 | 触边 **WordMult *= 2** | Word |

### 第四类：词头词尾 (Affix)
*主要影响 `RootWordBuff` 和 `WordMult`*

| ID | 名称 | 旧效果 | 新定义 (适配公式) | 逻辑层级 |
|:---|:---|:---|:---|:---|
| D01 | 起跑器 | 元音开头+5 | 元音开头 **RootWordBuff += 5** | Word |
| D02 | 终结技 | S结尾 x1.2 | S结尾 **WordMult *= 1.2** | Word |
| D03 | 押韵字典 | ING结尾 x1.5 | ING结尾 **WordMult *= 1.5** | Word |
| D04 | 贵族头冠 | 稀有开头 x3 | 稀有开头 **WordMult *= 3** | Word |
| D05 | 回文镜 | 回文 x5 | 回文 **WordMult *= 5** | Word |
| D06 | 重头戏 | 首字母 x2 | 首字母 **PercentMult *= 2** | Letter (Context) |
| D07 | 豹尾 | 尾字母 x2 | 尾字母 **PercentMult *= 2** | Letter (Context) |
| D08 | ER工会 | ER结尾+8 | ER结尾 **RootWordBuff += 8** | Word |

### 第六类：特殊 (Special)
| ID | 名称 | 旧效果 | 新定义 (适配公式) | 逻辑层级 |
|:---|:---|:---|:---|:---|
| F04 | 连胜奖杯 | 上回合>100 +0.5 | 若触发 **WordMult += 0.5** (全局) | Word |
| F05 | 赌徒骰子 | 随机倍率 | **WordMult *= (1.5 或 0.8)** | Word |

---

## 3. UI 表现需求
*   **绿色高亮**：如果 `LetterFinal > Base`，则字母卡片右下角分数值显示为绿色。
*   **实时计算**：拖动过程或放置后，需实时调用 `getLetterScore` 获取展示用的分数。
*   **橘色光晕**：对于交叉点的字母卡片外观光晕显示为橘色。
