# 拼词肉鸽：子路径静态站点（由 server {} 内 include）
# 部署目录默认：/srv/pingchi/

location = /pingchi {
    return 301 http://82.156.225.73/pingchi/;
}

location /pingchi/ {
    alias /srv/pingchi/;
    index index.html;
}
